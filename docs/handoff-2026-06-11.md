# 인수인계 — 2026-06-11 (구성원 페이지 UX 정비 + 전역 모션 + 내부 베타 시작)

> **세션 범위**: 구성원(/team) 검색·상태필터·리스트 표시 수정 + '보고대상 지정하지 않음' 플래그 신설 + 직무 표시 + 전역 팝업/드롭다운 마이크로모션 + 로그인 흐름 검증 + 리더 대상 내부 베타 오픈
> **총 commit**: 3건 — `91767e3`, `9ae7c28`, `c1aacbe` (모두 main push 완료 → Vercel 배포)
> **검증**: build(tsc+vite) / test 59 / lint(새 에러 0, 기존 `forceExpandCmd` 1건만) ✅
> **시트 변경**: 신규 컬럼 `보고대상지정안함` (GAS `upsertRow` 가 첫 저장 시 자동 생성 — 별도 작업 불필요)

---

## 1. 프로젝트 현재 상태

- 위치: `/Users/makestar/review-mvp` (Vite + React 19 + TS + Tailwind, Google Sheets DB + Apps Script, Vercel)
- 브랜치 `main`, 최신 `c1aacbe`. 작업트리 깨끗, `origin/main` 동기화 완료.
- SPREADSHEET_ID = `138NMXPcwrG_lOIkC27BGtTZLN-3Ql3mVOttvM5xD-mg`
- Apps Script 소스: `~/Desktop/apps-script/ReviewFlow.gs` (별도 repo, 수동 배포) — 이번 세션 `USER_HEADERS` 에 `보고대상지정안함` 추가(소스 정합용, 런타임은 auto-add 라 재배포 불필요)
- 개발 서버: 5174 (vite 기본 5173, 점유 시 증가)

---

## 2. 이번 세션 작업

### 2-1. 구성원 검색 한글 IME 자모 분리 수정 — `91767e3`
- **증상**: `/team` 검색창에 한글 입력 시 자모가 분리됨("산단"→"ㅅㅏㄴ단").
- **원인**: 검색 input 이 `useSetPageHeader`(context + useEffect)를 경유해 렌더 → value 갱신이 한 박자 늦게 돌아오며 IME 조합 버퍼를 끊음.
- **수정**: `MemberSearchInput` 컴포넌트 분리 — 표시값은 자체 local state 로 동기 갱신, `compositionstart/end` 인지로 조합 중에는 부모 필터(`search`)를 건드리지 않고 조합 종료 시 1회 커밋. 영문/숫자는 기존처럼 즉시 반영. `matchesSearch` 양변 `.normalize('NFC')` 처리.

### 2-2. 재직/휴직/퇴사 상태 필터 정상화 — `91767e3`
- **원인(다중 버그)**:
  1. `isUserActive()` 가 `leave_long`(장기휴직)을 비활동으로 묶음 → `panelUsers` 가 `activeUsers` 기반이라 장기휴직자가 **모든 필터에서 누락**.
  2. '휴직' 필터가 `activeUsers`(leave_long 없음)에서 걸러 단기휴직만 노출.
  3. '퇴사' 필터가 `showTerminated` 로 모집단을 통째 교체 → 선택 조직 무시.
  4. 카운트/전체선택/빈상태가 `displayedUsers` 가 아닌 `panelUsers` 기준 → 필터 0건 시 빈 리스트가 조용히 렌더.
- **수정**:
  - `userCompat.ts` 에 **단일 분류 헬퍼** `userIsWorking`/`userIsOnLeave`/`userIsTerminated`(activityStatus 기준 + legacy isActive 폴백) 신설. ⚠️ 상태 필터는 `isUserActive` 가 아니라 이 헬퍼 사용.
  - **scope ↔ status 분리**: `panelUsers`(조직/소속없음/전체 모집단, 휴직·퇴사 포함) → `applyStatusFilter`(=`displayedUsers`) 가 상태 narrowing. 조직 선택 안에서도 퇴사/장기휴직 필터가 정확히 동작.
  - **`showTerminated` 를 `statusFilter==='terminated'` 파생값으로** 변경(별도 state 제거 → desync 제거).
  - **필터 정의**: 전체=재직만(휴직·퇴사는 각 카테고리에서만 — **사용자 결정**) / 재직=active·other·legacy / 휴직=단기+장기 / 퇴사=terminated / 보고대상없음=재직중+managerId 없음.
  - 카운트·전체선택·빈상태 `displayedUsers` 기준 통일 + 필터 0건 "'OO' 상태의 구성원이 없습니다 + 필터 초기화" 안내.
  - 회귀테스트 `userCompat.test.ts` 8건 신설(leave_long 누락 회귀 포함) → 총 59.

### 2-3. '보고대상 지정하지 않음'(noManagerByDesign) 신설 — `91767e3`
- **목적**: 최상위 등 보고대상이 정상적으로 없는 사람을 '보고대상 없음' 경고/배지에서 제외.
- **타입**: `User.noManagerByDesign?: boolean`.
- **편집 UI**: `MemberEditDialog` "보고 대상" 아래 체크박스. 체크 시 보고대상 선택 비활성 + `managerId` 비움.
- **표시 제외**: `noReviewerIds`(배지 `hasReviewer`·'보고대상 없음' 필터·조직요약 카운트의 단일 소스)와 트리 노드 카운트(`noReviewerCount`)에서 `noManagerByDesign` 제외. `MemberProfileDrawer` 는 의도 플래그면 "보고대상 없음 (지정하지 않음)", 아니면 기존 경고 문구로 분기.
- **시트 영속**: `sheetWriter.toSheetRow` 에 `보고대상지정안함`('true'/''), `sheetParser` 에서 읽기. GAS `updateUser`→`upsertRow` 가 미지 컬럼을 extraKeys 로 자동 추가 → **GAS 재배포·수동 시트 편집 불필요**. 체크 해제 시 빈값으로 클리어.
- ⚠️ **구성원별 옵트인**: 자동으로 안 사라짐. 대상자를 편집→체크→저장해야 그 사람만 제외됨.

### 2-4. 리스트 표시 정비 — `91767e3` + `c1aacbe`
- **중복 제거**: 직책/역할이 이름 아래(좌측)와 우측 `orgTag`("{조직}·{역할}") 양쪽에 떠 중복 → 우측은 **소속 경로(조직)만** 표시, 직책은 좌측 1회. (`91767e3`)
- **hover 액션 → 더보기 메뉴**: 기존 hover 아이콘 4개(프로필/수정/마스터로그인/퇴사)를 DS `MsActionMenu`(`triggerVisibility="hover"`)로 교체. 권한 조건은 각 item `hidden` 으로 보존. (`91767e3`)
- **직무 표시**: 이름 아래 서브텍스트를 직책 단독 → **"직책 · 직무"**(jobFunction)로 확장. (`c1aacbe`)

### 2-5. 전역 팝업/드롭다운 마이크로모션 — `9ae7c28`
- **컨벤션**: 드롭다운/메뉴 `fadeSlideDown 0.15s` / 모달 backdrop `fadeIn 120ms`+카드 `scaleIn 140ms` / 드로어 backdrop `fadeIn`+패널 `slideInRight 180ms`(신규 keyframe).
- **적용(베이스 위주 → 다수 자동 커버)**: `MsActionMenu`, `UserSearchSelect`, Sidebar 워크스페이스 스위처, Feedback 자동완성 / `ModalShell`(17+ 모달), `ConfirmDialog`, `BulkManagerDialog`, `GlobalSearch` / `SideDrawer`(4+ 드로어).
- `prefers-reduced-motion: reduce` 존중 전역 규칙 추가. `MsSelect`(네이티브 select)는 제외.
- 빌드 산출 CSS 에 4개 keyframe 모두 포함 확인(Tailwind JIT arbitrary `animate-[...]` 정상).

### 2-6. 로그인 흐름 검증 (코드 경로) — 변경 없음
- **결론**: 이메일이 `_구성원` 에 있으면 **최초 로그인이든 재로그인이든 즉시 active** (별도 first-login 게이트 없음). GAS `verifyGoogleLogin` 이 `_구성원.이메일` 을 대기승인 큐보다 먼저 조회(`ReviewFlow.gs` matchUser).
- **경로**: Google SSO → `verifyGoogleLogin`(GAS, 도메인 makestar.com+email_verified 검증) → `{userId:사번, status:'active'}` → 클라가 `teamStore.users` 에서 `사번`/`이메일` 매칭 → `login()`(`Login.tsx:79-99`).
- **데이터 전제조건**(코드 아님): ① `_구성원.이메일` = 리더 실제 Google 계정(불일치 시 대기승인으로 빠짐) ② `사번` 비어있지 않음(빈 시 "userId 없음" 에러). migration_readiness(2026-06-10)에서 빈셀/이메일 0 → 충족 상태. **단, 이메일이 실제 Google 계정과 같은지는 실제 로그인으로만 확인 가능** → 리더 1명 production 스모크 테스트 권장.
- ⚠️ 실제 OAuth 로그인은 브라우저+실계정 필요라 헤드리스 e2e 미실행(코드 경로 추적으로 검증).

---

## 3. 운영/배포 상태

- **코드**: `c1aacbe` 까지 main push → Vercel 자동 배포. 작업트리 깨끗.
- **내부 베타 시작**: 리더 대상. **OAuth Client ID rotate 는 내부 베타라 보류(사용자 결정, 리스크 수용)** — 정식/외부 오픈 전 필수로 남김(git history `a52d65b` 노출).
- **"일부 반영 안 됨" 진단**(세션 말미 문의): 커밋/코드 누락 아님(전부 push·코드 완전). 후보 = ① production 캐시(하드 리프레시) ② noManagerByDesign 옵트인 오해(대상자 체크·저장 필요) ③ dev HMR stale(세션 말 5174 재시작 완료). 구체적 미반영 항목 지정 시 해당 경로 추적 필요.

---

## 4. 내일 할 일 / 잔여

1. **베타 스모크 테스트**: 리더 실계정 production 로그인 1회 → 홈 진입 OK / 대기승인으로 빠지면 그 사람 `_구성원.이메일` 정정.
2. **'보고대상 없음' 영속 확인**: 대상자 편집→'보고대상 지정하지 않음' 체크→저장→**새로고침/sync 후에도 배지 사라진 채 유지**되는지(=`보고대상지정안함` 컬럼 자동생성·round-trip 확인).
3. **OAuth Client ID rotate** (P0, 보류 지속) — 외부/정식 오픈 전 필수.
4. **P0-2 운영 데이터 점검** (admin 재푸시) — 시트 빈 셀 fallback 가능성. 리더가 빈 화면/누락 보고 시 이 케이스.
5. (보류) Supabase 이전 — 컴플라이언스 게이트(HR 국외 SaaS) 미결정. 상세 [[project_review_mvp_supabase_migration]]. 신규 컬럼 `보고대상지정안함`(=Postgres `users.no_manager_by_design boolean`) ETL 반영.

---

## 5. 참조

- **메모**: `project_review_mvp.md` / `project_review_mvp_supabase_migration.md`(신규 컬럼 반영 완료) / `feedback_gas_paste_workflow.md`
- **직전 handoff**: `docs/handoff-2026-06-10-supabase-prep.md`
- **lint 기존 에러**: Team.tsx 의 `forceExpandCmd` setExpanded-in-effect 1건(이번 세션 무관, HEAD 부터 존재).
