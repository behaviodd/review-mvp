# 인수인계 문서 — 2026-05-06 (UI 디테일 점검 + Apps Script 배포 완료)

> 지난주 핸드오프 4종 (`handoff-2026-04-29.md` 안정성 / `handoff-2026-04-29-design.md` 디자인 / `handoff-2026-04-29-security.md` 보안 / `handoff-2026-04-30.md` R8 자동 승인 / `handoff-2026-04-30-design-and-guide.md` 디자인 wrap-up + 가이드) 와 함께 보면 흐름 명확.
> 본 문서 = 일주일 만에 다시 들어온 세션. 미배포 Apps Script 배포 확인 + 마이너 UI 디테일 점검 시작.

---

## 1. 프로젝트 현재 상태

- **위치**: `/Users/makestar/review-mvp` · 브랜치 `main` · origin 동기화 완료 (`1ae2aa8`)
- **단계**: **배포 완료 후 마이너 UI 디테일 점검 라운드 진입**
- **DB**: Google Sheets 유지 (변동 없음)
- **Apps Script**: ✅ **배포 완료** (사용자 확인). 일주일간 누적된 미배포 변경 (`fadbc0e` audit dedupe + R8 자동 승인 + `approveMember` 의 `jobFunctionA`/`managerIdA`) 모두 production 반영됨
- **OAuth Client ID rotate**: ⏸️ 사용자 결정으로 보류 (추후 처리). git history `a52d65b` 노출 상태 유지. 외부 시연 시점 전 처리 필수
- **Vercel**: 자동 배포 — `462cae3` / `1ae2aa8` 모두 main push 시 트리거
- **Dev 서버**: 5174 백그라운드 실행 중 (`http://localhost:5174/`)
- **마지막 검증**: build 722ms 그린 / 36/36 tests / lint clean (변경 라인) / dev 5174 200

### 본 세션 commit 흐름 (최신 → 과거)

```
1ae2aa8 fix(audit-log): '전체' 탭을 맨 앞 + default 로 변경
462cae3 refactor(ui): MsControl label 080→fg-default 통일 (Phase D-3.J 후속)
```

(이전 13건 = 2026-04-30 후반부 D-3.J~D-3.M-fix6 — `handoff-2026-04-30-design-and-guide.md` 이후, `e1d438c` ~ `fcebfeb` 까지 push 완료된 기록)

---

## 2. 본 세션 작업

### 2-1. 컨텍스트 재파악 (commit 없음)

일주일 만에 재진입 — 이전 핸드오프 5종 + git log 30건 + post-handoff 13개 commit 본문 검토. 메모리 (`project_review_mvp.md` / `feedback_collaboration_style.md` / `reference_apps_script_location.md`) 모두 재확인.

### 2-2. D-3.J 후속 시각 회귀 스캔 (commit 없음, 결과 → 2-3 으로 이어짐)

옵션 A 진행 — `text-base` 마이그레이션 후 자연 발생할 수 있는 시각 회귀 후보를 코드 레벨에서 식별.

**스캔 결과 — 의도된 예외 (수정 불필요)**:
- LNB `text-sm` 4건 (Sidebar 브랜드 / 메뉴 / 드롭다운) — D-3.J 명시 예외
- `text-base + leading-5 + h-7` segmented control (OpsFilterBar / ListToolbar / MsButton md / GuideSidebar nav) — 의도
- MsInput md = `py-2 + text-base` (h≈40px) — 비례 적정
- 멀티라인 `text-base` (MyReviewWrite / TeamReviewWrite / Feedback `whitespace-pre-wrap`) — 모두 `leading-relaxed` 명시되어 있음

**raw `text-gray-*` 분포 — 디자이너 결정 대기 (보류)**:

| 토큰 | 횟수 | 추정 의미 | hex 차 (가능 매핑) |
|---|---|---|---|
| `text-gray-080` | 99 | 본문 강조 | `fg-default` -9 RGB (안전) |
| `text-gray-070` | 73 | 본문 | `text-text-primary` -25 RGB |
| `text-gray-060` | 75 | 메타·subtle | `text-text-secondary` +60 RGB ⚠️ |
| `text-gray-030` | 51 | divider·placeholder | 매핑 없음 |
| `text-gray-020` | 4 | divider | 매핑 없음 |

**분포 — 30+ 파일** (TemplateBuilder / CycleNew·Edit·List / MyReviewWrite / TeamReviewWrite / Dashboard / Reports / Settings / Notifications / Goals / Feedback / Permissions 등 주요 페이지 거의 전부).

→ 일괄 매핑 phase 는 디자이너 결정 후. 본 세션은 **컨트롤 라이브러리 한정** 만 정리 (다음 항).

### 2-3. MsControl label 080→fg-default 통일 (`462cae3`) — Phase D-3.J 후속

**배경**: D-3.J 본문 16px 마이그레이션 후속 점검에서, 컨트롤 라이브러리만 raw `text-gray-080` 사용 — 같은 페이지 안 다른 본문은 `text-fg-default` 정합되어 있어 미세 톤 차이.

**변경 (`src/components/ui/MsControl.tsx` — 3곳, replace_all)**:
- L71 MsCheckbox label
- L111 MsRadio label
- L163 MsSwitch label
  - `'text-base text-gray-080'` → `'text-base text-fg-default'`
  - `disabled` 분기 (`text-fg-subtlest`) 그대로

**영향**:
- 컨트롤 label 톤이 페이지 본문과 정합 (-9 RGB → 거의 무차)
- 호출처 코드 변경 없음
- `MsInput` label (L201) 의 `text-gray-060` 은 -15 RGB 위험으로 보류

**미적용 (별도 phase)**: § 2-2 의 raw 080/070/060 일괄 매핑 — 디자이너 결정 후.

### 2-4. AuditLog '전체' 탭 맨 앞 + default 변경 (`1ae2aa8`)

**배경**: 사용자 명시 — "감사 로그의 '전체' 항목을 맨 앞 탭으로 옮겨주세요". 첫 탭과 default 가 일치하지 않아 진입 시 두 번째 탭이 선택된 상태처럼 보이는 미세 모순.

**변경 (`src/pages/AuditLog.tsx`)**:
- L54: `useState<TabKey>('impersonation')` → `'all'`
- L58~69 headerTabs 순서 재배열: 전체 → 마스터 로그인 → 시스템 액션 (전체가 맨 앞)

**옵션 결정**: B (탭 순서 + default 둘 다) — 첫 탭 = default 일관성 우선

---

## 3. 결정·정책 변경

- **OAuth Client ID rotate**: 사용자 결정으로 **보류** (외부 시연 시작 전 처리). 그 전까지 git history `a52d65b` 의 ID 가 유효 — 외부 노출 금지
- **MsControl 컨트롤 label 톤**: `text-fg-default` 표준 (페이지 본문과 정합)
- **`/audit` 진입 시 default 뷰**: `'all'` (빙의 + 시스템 통합)

---

## 4. 잔여 작업 (우선순위별)

### 4-1. 외부 시연 / 도입 협상 시작 전 P0

- ⚠️ **OAuth Client ID rotate** (보류 중) — Cloud Console → 새 ID → Script Properties + `VITE_GOOGLE_CLIENT_ID` 동시 갱신 + Apps Script 재배포
- Owner 3명 지정 (release / operations / maintenance) — 가이드 § 1
- Sentry + Vercel Analytics 통합 — 관측성 E 그룹
- Vercel cost quota 알람

### 4-2. 가까운 우선순위

1. **UI 모순점·디테일 point fix** (이번 세션에서 시작 — 사용자가 case-by-case 발견하는 대로 fix)
2. **`isSystemOperator()` 함수 본체 제거** — 호출처 0, 안전한 cleanup (`src/utils/permissions.ts` L39)
3. **자동 승인 후처리 UI**:
   - Settings 토글 카드에 "자동 승인된 보강 필요 멤버 N명" 카운트
   - `/team` "보강 필요" 필터 탭 (orgUnitId 빈값 + 사번 prefix `auto_`)
   - `migrateUserId({oldId, newId, modifierId})` action — 자동 승인된 `auto_*` 사번을 정식 사번으로 변경 + 권한그룹/평가권자/제출 모두 갱신
4. **CycleNew preview row 의 label-value 정보 표** — D-3.I 미적용. 시각 토큰화 검토

### 4-3. 멀리 (디자이너 결정 후 phase 분리)

5. **raw `text-gray-080/070/060` 일괄 매핑** — 새 토큰 (`fg-strong` / `fg-medium`) 정의 또는 시각 회귀 검토 후 일괄 매핑. 분포는 § 2-2 표 참조
6. **B 그룹 audit 잔여** (B2 / B7 / B8 / B11 / B12) — `docs/stability-audit-B.md` 167줄 참조. P2 부채

---

## 5. 함정 / 주의

1. **OAuth Client ID 노출 상태 — 외부 시연 금지** — § 4-1 처리 전. 현재 사용자 결정으로 보류
2. **Apps Script 위치 = `~/Desktop/apps-script/` (git 비추적)** — main repo 안에 `apps-script/` 폴더 만들지 말 것. canonical 은 Apps Script 편집기 (Google 서버)
3. **raw 토큰 매핑 시 hex 차 검토 필수** — 080(-9) 안전, 070(-25)·060(+60) 시각 회귀 가능. 일괄 변환 전 디자이너 합의
4. **`/audit` 본문 분기는 그대로** — `tab === 'all'` 일 때 빙의 + 시스템 두 섹션 모두 노출이 기존 의도와 일치 (변경 없음)
5. **MsControl L201 INPUT label `text-gray-060`** — 본 세션에서 손대지 않음. 일괄 매핑 phase 에서 검토
6. **lint warning 1건** (`AuditLog.tsx` L79 — unused eslint-disable directive) — 본 commit 범위 밖. 무관한 정리 시 함께 제거 가능
7. **세션 시작 시 메모리 `feedback_figma_to_code.md` / `project_review_mvp.md` / `feedback_collaboration_style.md` / `reference_apps_script_location.md` 모두 읽기** — 이전 결정 누락 방지

---

## 6. 검증 명령어

```bash
# 빌드 + 타입 체크
npm run build

# 단위 + 통합 테스트
npm test

# 타입체크만 (빠름)
npx tsc --noEmit

# 변경 파일 lint
npx eslint src/path/to/changed.tsx

# Dev 서버 (5174)
npm run dev -- --port 5174

# pre-commit 시크릿 스캐너 (husky 자동 실행)
bash scripts/check-secrets.sh
```

---

## 7. 체크리스트 — 새 에이전트 첫 행동

- [ ] `git log --oneline -15` 으로 최근 흐름 파악 (본 세션 2건 + 그 전 13건 + 그 전 핸드오프)
- [ ] **이전 핸드오프 6종 모두 읽기** (4-29 안정성/디자인/보안 + 4-30 R8/디자인 wrap-up + 본 문서)
- [ ] `docs/runbooks/script-properties.md` 정독 — 시크릿 운영 규칙
- [ ] `docs/ui-tokens.md` 정독 — 디자인 시스템 (특히 § 2 타이포 / § 4-3 페이지 표면 / § 7.6~7.8 시트형·트리·Tab strip)
- [ ] `docs/stability-audit-B.md` — B 그룹 audit 잔여 5건
- [ ] `npm run build && npm test` baseline 그린 확인
- [ ] dev 서버 5174 띄우기 (배경)
- [ ] § 4-2 의 우선순위 항목 사용자와 합의 후 진행

---

## 8. 변경 파일 (본 세션, 누적)

```
src/components/ui/MsControl.tsx                           (D-3.J 후속 — 080→fg-default)
src/pages/AuditLog.tsx                                    (탭 순서 + default 변경)
docs/handoff-2026-05-06.md                                (이 문서)
```

---

## 9. 다음 에이전트가 알아야 할 핵심 결정 (Quick reference)

- **Apps Script 배포 완료** (2026-05-06) — 누적된 미배포 변경 모두 production 반영됨
- **OAuth Client ID rotate 보류** — 외부 시연 시작 전 사용자가 처리
- **MsControl label 톤 표준 = `text-fg-default`** — 컨트롤 라이브러리 한정 적용
- **`/audit` default tab = `'all'`** — 첫 탭 = default 일관성
- **raw text-gray-080/070/060 일괄 매핑은 디자이너 결정 후** — 분포 30+ 파일, 247건 (080+070+060). 새 토큰 도입 가능성 검토
- **현재 모드**: 마이너 UI 디테일 점검 라운드 — 사용자가 case-by-case 발견하면 즉시 fix → 검증 4종 → commit 단위로 push

---

작성: 2026-05-06 · 본 세션은 일주일 만의 재진입 + 마이너 디테일 점검 시작. 새 에이전트는 § 7 체크리스트로 시작 권장.
