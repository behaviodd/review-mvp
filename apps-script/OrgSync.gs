/**
 * OrgSync.gs — 이 파일의 모든 기능은 Code_v2.gs에 통합되었습니다.
 *
 * ★ 이 파일의 내용은 비워야 합니다 ★
 *
 * Code_v2.gs 와 같은 프로젝트에 있을 경우, 아래 함수들이 충돌합니다:
 *   doGet, doPost, upsertRow, getSheet, sheetToObjects
 *
 * 충돌 시 OrgSync.gs 버전이 Code_v2.gs 버전을 덮어써서
 * Code_v2.gs의 patchRow, updateUser 수정이 무효화됩니다.
 *
 * → Google Apps Script 편집기에서 이 파일을 열고 내용을 전부 삭제하거나,
 *   프로젝트에서 이 파일을 삭제하세요.
 */
