# 인수인계 문서 — 2026-05-18 (마이너 UI 디테일 4라운드 + 참가자 관리 + Apps Script 분기 미배포)

> 직전 핸드오프 `handoff-2026-05-06.md` 이후 12일치 누적분 wrap-up.
> 본 문서 = "마이너 UI 디테일 case-by-case fix" 모드에서 4개 라운드(읽기 색 / 이전 답변 채우기 / Goals 진입점 add→revert / 사이클 진행 중 참가자 관리) 진행 후 push 보류 상태의 정리.

---

## 1. 프로젝트 현재 상태

- **위치**: `/Users/makestar/review-mvp` · 브랜치 `main` · **origin 대비 8 commit ahead (미푸시)**
- **워킹 트리**: clean
- **단계**: 마이너 UI 디테일 case-by-case fix 모드 지속. 4라운드 완료, push 1건 대기
- **DB**: Google Sheets 유지 (변동 없음)
- **Apps Script**: ✅ **신규 작업 불필요** (§ 2-5 사후 검증). 라운드 4 의 클라이언트 액션이 호출하는 `upsertCycle` / `upsertSubmission` / `appendAudit` 모두 기존 doPost 분기로 처리됨. **push 후 사용자가 앱 사용 시 syncQueue 자동 flush → production 반영**. 그 외 누적분은 2026-05-06 배포 시 모두 production 반영됨
- **OAuth Client ID rotate**: ⏸️ 사용자 결정으로 계속 보류
- **Vercel**: 자동 배포 대기 (push 전이라 미배포)
- **마지막 검증** (`6c0b28b` 시점): build 583ms 그린 / 36/36 tests / lint clean / dev 5174 200

### 본 세션 commit 흐름 (최신 → 과거, 모두 미푸시)

```
6c0b28b fix(cycle): 제외된 참가자가 OpsCenter 리스트에서 사라지지 않는 문제
88a67b3 fix(cycle): 참가자 추가/제외 게이트를 submission 기반으로
6181a31 refactor(cycle): 참가자 관리를 OpsCenter 에 통합 — 대상자 리스트 인라인 제외
e13eea4 Revert "feat(goals): /goals 라우트 + Sidebar 메뉴 노출"
c90206a feat(cycle): 진행 중 참가자 추가·제외 (클라이언트)
0f9178b feat(my-review): 질문별 '이전 답변 채우기' 버튼 — 자기평가 이전 회차 재사용
d320b16 feat(goals): /goals 라우트 + Sidebar 메뉴 노출 [REVERTED]
c21e0e0 fix(review): 제출 완료 readonly 답변 본문 색 진하게 (text-fg-default)
```

---

## 2. 본 세션 작업 — 4 라운드

### 2-1. 라운드 1 — 제출 완료 readonly 답변 색 (`c21e0e0`)

**배경**: 사용자 보고 — 자기평가/팀평가 제출 완료 detail 의 답변 본문이 회색톤으로 흐려서 가독성 떨어짐.

**원인 2가지**:
1. `MyReviewWrite` QuestionCard 의 readOnly text 분기가 `MsTextarea(disabled)` 사용 → `MsControl.tsx:249` 의 `disabled:text-fg-subtlest` (#8a99a8) 적용
2. `InputAnswerContent` / `FlatAnswerContent` + `TeamReviewWrite` readonly 본문이 raw `text-gray-070` (#3b454f) — handoff-2026-05-06 § 2-2 의 73건 중 본문 영역만 핀포인트 정리

**변경**:
- `MyReviewWrite.tsx` — QuestionCard text 분기를 `MsTextarea(disabled)` → `<p>` 로 전환 (TeamReviewWrite 패턴과 통일). 본문 색 `text-gray-070` → `text-fg-default`
- `TeamReviewWrite.tsx` — selfAnswer / readonly answer 본문 색 동일 매핑

**안전성**: handoff § 4-3 raw text-gray-070 일괄 매핑 정책과 충돌 없음 — readonly "답변 본문" 만 핀포인트. 미응답 placeholder (`text-fg-subtlest`) / rating 강조 (`text-gray-070`) 는 의미적 분리 유지.

### 2-2. 라운드 2 — '이전 답변 채우기' 버튼 (`0f9178b`)

**배경**: 사용자 보고 — '내 작성' 화면에서 전 회차 답변을 불러올 방법 없음. 새 사이클마다 처음부터 작성.

**핵심 제약**: `TemplateQuestion.id` 는 사이클마다 신규 생성 → 같은 의미 질문도 ID 가 다름. 자동 매핑 불가.

**신설 utility (`src/utils/previousAnswers.ts`) — `mapPreviousAnswers()`**:
- 1순위: `question.text` 정확 일치 (trim) — 의미적 동일성
- 2순위 fallback: `(section order index, question.order)` 위치 일치 — 텍스트가 약간 변경되었거나 sectionId 가 새 발급된 경우
- 매칭 안 되면 Map 미포함 → 호출처에서 버튼 비활성

**UI**:
- `QuestionCard` 헤더 우측에 인라인 "↺ 이전 답변 채우기" 버튼 + tooltip 에 이전 사이클 제목
- `canPrefill` 조건: `!readOnly && previousAnswer 존재 && 현재 answer 가 모든 필드 비어 있을 때` — 의도와 다른 내용 덮어쓸 위험 차단
- 클릭 시 `question.type` 에 맞는 필드만 채움 (text/rating/competency/multiple_choice 모두 지원)

**이전 사이클 조회** (`MyReviewWrite` main):
- 같은 `reviewee + reviewer + type='self' + status='submitted'` 의 다른 cycleId 중 `submittedAt` 최신 1개
- `getEffectiveTemplate(prevCycle, templates)` 로 이전 템플릿 확보 후 mapping
- `useMemo` 로 submissions / cycles / templates 변경 시 재계산

**범위**: `MyReviewWrite` 만 (사용자 요구 "리뷰 '내 작성'" 한정). `TeamReviewWrite` 동일 패턴 확장은 별도 phase.

### 2-3. 라운드 3 — Goals 진입점 추가 후 revert (`d320b16` → `e13eea4`)

**v1 (`d320b16`)**: `/goals` 라우트 + Sidebar "목표 관리" 메뉴 추가. 피평가자가 본인 목표를 입력할 진입점 부재 → ReviewerReferenceRail (cycle.referenceInfo?.includeGoals=true) 가 항상 placeholder.

**v1 한계 (commit 본문에 명시)**: `goalStore` 가 localStorage 전용 → 본인 PC 에만 저장. 평가자(다른 PC) 가 못 봄.

**revert (`e13eea4`, 사용자 결정 2026-05-18)** — Goals 영역은 별도 상시 진입점 X. **리뷰 운영(템플릿 작성) 영역에서 1회성으로** 통합하는 방향으로 전환.

revert 내용:
- `App.tsx` — `import { Goals }` + `<Route path="goals">` 제거
- `Sidebar.tsx` — `MsStarIcon` import + "목표 관리" nav item 제거
- `src/pages/Goals.tsx` 페이지 컴포넌트 자체는 **보존** (향후 통합 시 재활용)

### 2-4. 라운드 4 — 사이클 진행 중 참가자 추가·제외 (4 commit)

**배경**: 사용자 보고 — 사이클 발행 후 중도 퇴사자/신규 합류 인원 변경 방법 없음. 발행 시점 `createCycleSubmissions` 일괄 생성한 submission 집합이 굳어 있었음.

#### 2-4-a. 클라이언트 action + 별도 패널 (`c90206a`)

**타입 추가** (`src/types/index.ts`):
- `AuditAction` 에 `'cycle.participant_added'`, `'cycle.participant_removed'` 추가

**`reviewStore` action**:
- `addCycleParticipant(cycleId, userId, actorId)`
  - 상태 게이트: draft / closed 차단
  - `targetMode` 동결 정책: custom 이 아니면 현재 `resolveTargetMembers` 결과를 `targetUserIds` 에 모두 넣고 `targetMode='custom'` 으로 전환 → 이후 mode 의존 로직 제거
  - `createCycleSubmissions` 동일 로직으로 1명 array 호출 → self / downward (`cycle.downwardReviewerRanks`) / upward 생성. peer 는 admin/leader 이후 배정 (기존 정책)
  - `cycleWriter` / `submissionWriter` 로 sheets sync 큐 push
  - `recordAudit` 호출
- `removeCycleParticipant(cycleId, userId, actorId, reason?)`
  - 동일 게이트
  - `targetUserIds` 에서 제거 + targetMode 동결
  - 해당 user 가 `revieweeId` 인 submission 중 `status !== 'submitted' && !autoExcluded` 인 것만 `autoExcluded.reason='removed'` 마크 (제출 완료는 보존, **삭제 안 함**)

**UI (별도 패널)**: `CycleParticipantsPanel.tsx` 신설. CycleDetail 의 OpsCenter 위에 배치.

**AuditAction 추가에 따른 동기화**: `AuditLogDrawer.tsx` + `pages/AuditLog.tsx` 의 라벨 / tone 매핑 ('참가자 추가' green / '참가자 제외' orange).

#### 2-4-b. OpsCenter 통합 (`6181a31`) — 별도 패널 제거

**사용자 결정**: 별도 패널 폐기, OpsCenter 와 통합. 추가 = 헤더 / 제외 = 대상자 리스트 행 인라인.

변경:
- 신설 `AddParticipantModal.tsx` / `RemoveParticipantModal.tsx` (기존 패널 내부 로직 분리)
- `OpsTable.tsx` — `onRowRemove?: (row: OpsRow) => void` prop 추가. 마지막 그리드 컬럼에 32px 액션 셀 (group-hover/focus 시만 X 아이콘 노출)
- `OpsCenter.tsx` — `usePermission().can.manageCycles + cycle.status (!draft && !closed)` 조합 `canManageParticipants` 게이트. 헤더 ("사이클 재푸시" 다음) 에 "참가자 추가" 버튼. 행 인라인 제외는 `perspective === 'reviewee'` 일 때만
- `CycleDetail.tsx` — `CycleParticipantsPanel` import / 사용 제거
- `CycleParticipantsPanel.tsx` **삭제**

#### 2-4-c. 게이트 진실의 원천 통일 (`88a67b3`)

**버그**: OpsCenter 에서 행 제외 클릭 시 "참가 중이 아닌 사용자입니다." 토스트로 거부됨. 실제로는 submission 존재.

**원인**: `addCycleParticipant` / `removeCycleParticipant` 의 "참가 중" 판정이 `resolveTargetMembers(cycle, users)` 기준. `cycle.targetMode` 가 `'org'`/`'manager'` 인 경우 현재 users 의 부서·매니저 값으로 매번 재계산 → 발행 후 user 가 부서를 옮기거나 매니저가 바뀌면 효과적 멤버에서 빠짐. 그러나 submission 은 발행 시점 생성. → 리스트(submission 기반) vs 게이트(mode 기반) 불일치.

**수정**:
- 게이트를 `state.submissions.some(s => s.cycleId === cycleId && s.revieweeId === userId && !s.autoExcluded)` 로 변경
- `AddParticipantModal.tsx` 의 `currentMemberIds` 계산도 동일하게 submission 기반으로 통일
- `resolveTargetMembers` import 제거

**진실의 원천 = submission** 으로 통일. `targetUserIds` 갱신 로직(동결 + custom 전환) 은 그대로 — "발행 시점 의도 보존" 목적.

#### 2-4-d. 제외 후 리스트 잔존 버그 (`6c0b28b`)

**버그**: 제외 처리해도 OpsCenter 대상자 리스트에서 행이 계속 보임. 토스트는 성공, audit 도 기록.

**원인**: `removeCycleParticipant` 정책 = submission 삭제 X, autoExcluded 마크만. 그러나 `buildOpsRows` (`utils/opsCenter.ts:135`) 의 `cycleSubs` 필터링이 cycleId 만 보고 autoExcluded 무시.

**수정**: `cycleSubs` 필터에 `&& !s.autoExcluded` 추가. 행 자체 사라짐.

**연쇄 영향**: `collectPendingSubmissionIds` 는 `buildOpsRows` 결과 사용 → 리마인드/재푸시/연장 모든 bulk 액션이 자동으로 제외된 자를 건너뜀 (일관).

**미처리** (별도 phase 후보):
- "제외된 참가자 보기" 토글 (admin audit 차원)
- `computeKpis` 의 `autoExcluded` 분모 보정 (현재 KPI 에 포함되어 분모/분자 반영)
- 재활성화 (autoExcluded 해제 + 기존 submission 복원)

### 2-5. 사후 검증 — Apps Script 신규 작업 불필요 확인

**배경**: 라운드 4 의 4 commit body (`c90206a` / `6181a31` / `88a67b3` / `6c0b28b`) 가 "다음 commit: Apps Script `addCycleParticipant` / `removeCycleParticipant` doPost 분기 추가 + 사용자 별도 배포" 를 다음 단계로 명시. 본 핸드오프 작성 후 정밀 검증한 결과 **잘못된 가정** 확인.

**검증 결과** — 클라이언트가 호출하는 sync 액션은 모두 기존 분기로 처리됨:

| 호출 (`reviewStore.ts`) | sync 액션 (`reviewSheetWriter.ts`) | Apps Script doPost 분기 |
|---|---|---|
| `cycleWriter.upsert(updated)` L266 / L328 | `upsertCycle` | **L1099** — `upsertRow(SHEET_CYCLES, ...)` |
| `submissionWriter.upsert(sub)` L271 / L346 | `upsertSubmission` | **L1139** — `upsertRow(SHEET_SUBMISSIONS, ...)` |
| `recordAudit({...})` L274 / L348 | `appendAudit` | **L1153** — dedupe 포함 append |

**근거**:
- `upsertCycle` 가 `targetMode='custom'` + 갱신된 `targetUserIds` 를 row 단위로 덮어씀 → 라운드 4 의 cycle 변경 자동 반영
- `upsertSubmission` 가 신규 submission 추가 + 기존 submission 의 `자동제외JSON` 갱신을 row upsert 로 동일 처리
- `appendAudit` 가 신규 `AuditAction` enum 값 (`cycle.participant_added` / `cycle.participant_removed`) 을 enum 검증 없이 그냥 '액션' 컬럼에 문자열 저장 — 추가 작업 불필요

**결론**: 4 commit body 의 "다음 commit: Apps Script doPost 분기 추가" 는 **무효**. 8 commit `git push` 만 하면 사용자가 다음에 앱 사용 시 syncQueue 자동 flush → production Sheets 반영.

**push 후 검증 절차** (사용자):
1. 사이클 1건에서 참가자 추가 + 제외 각 1회
2. Google Sheets 의 '평가사이클' 시트 — 해당 cycle 행의 `대상모드` = `custom`, `대상사용자IDS` 갱신 확인
3. '제출' 시트 — 신규 submission 행 / 기존 submission 의 `자동제외JSON` 갱신 확인
4. '감사로그' 시트 — `cycle.participant_added` / `cycle.participant_removed` 행 추가 확인

---

## 3. 결정·정책 변경

- **Goals 진입점**: 별도 메뉴 / 상시 라우트 **X**. 리뷰 운영(템플릿 작성) 안에서 1회성 통합으로 전환 (`e13eea4`). `goalStore` Sheets 동기화는 이 통합 phase 에서 같이 해소
- **사이클 참가자 관리 UI**: 별도 패널 X. **OpsCenter 통합** (헤더 추가 + 대상자 행 인라인 제외). `perspective === 'reviewee'` 일 때만 인라인 제외 노출
- **참가자 게이트의 진실의 원천 = submission**. `resolveTargetMembers` 는 게이트 용도로 사용 금지 (mode 'org'/'manager' 시 발행 후 인원 변동으로 오판). 발행 시점 의도 보존용으로만
- **제외 정책 = autoExcluded 마크 (삭제 X)**. **제출 완료 건 포함 모든 submission 에 마크** (2026-05-18 정정 — 초기 정책의 "제출 완료 건은 마크 안 함" 으로 인한 UX 모순 = 토스트 success 인데 행 잔존 해소). `autoExcluded` 의 의미 = **"운영 리스트에서 숨김" 마커**. 평가 본체 / answers / 평점은 그대로 보존되며 평가 자체의 유효성도 유지 (KPI / Reports 등은 마크 무관). 운영 리스트(`OpsCenter`) 는 `buildOpsRows` 의 `!autoExcluded` 필터로 자연 숨김
- **사이클 발행 후 인원 변경 시 `targetMode='custom'` 으로 동결** — 이후 mode 의존 재계산 차단
- **'이전 답변 채우기' 매칭 정책**: text 정확일치 1순위 / (section order, q.order) fallback. `canPrefill` strict (현재 answer 전 필드 비어있을 때만) → 덮어쓰기 차단
- **readonly 답변 본문 색**: `text-fg-default` 표준 (raw text-gray-070 / disabled text-fg-subtlest 배제). 단 미응답 placeholder, rating 강조는 톤 분리 유지

---

## 4. 잔여 작업 (우선순위별)

### 4-1. 본 세션 직후 P0

1. **9 commit `git push`** — 사용자 합의 시 일괄 push (라운드 4 의 4 commit + readonly 색 / 이전 답변 채우기 / Goals revert + 본 핸드오프 + 정정 commit)
2. ~~Apps Script doPost 분기 작성 + 배포~~ — **§ 2-5 사후 검증으로 무효화**. 신규 분기 불필요. push 후 사용자가 앱 사용 시 syncQueue 자동 flush → production 반영. push 후 Sheets 직접 확인 절차는 § 2-5 마지막 참조

### 4-2. 외부 시연 / 도입 협상 시작 전 P0 (이월)

- ⚠️ **OAuth Client ID rotate** (계속 보류) — Cloud Console → 새 ID → Script Properties + `VITE_GOOGLE_CLIENT_ID` 동시 갱신 + Apps Script 재배포
- Owner 3명 지정 (release / operations / maintenance)
- Sentry + Vercel Analytics 통합 — 관측성 E 그룹
- Vercel cost quota 알람

### 4-3. 가까운 우선순위

3. **Goals 통합 phase** (라운드 3 revert 후속) — TemplateBuilder 또는 CycleNew 의 사이클 발행 흐름에서 Goals 1회성 입력·참조 UI. `goalStore` localStorage 한계(평가자가 못 봄)도 같이 해소 (Sheets 동기화 또는 cycle 내부 데이터 흡수)
4. **'이전 답변 채우기' TeamReviewWrite 확장** — 같은 utility 재사용. self 한정 → peer/upward/downward 도 적용 가능성 검토 (대상자 변경되면 매칭 의미 모호)
5. **참가자 관리 후속 3건**:
   - "제외된 참가자 보기" 토글 (admin)
   - `computeKpis` 의 autoExcluded 분모 보정
   - 재활성화 정책 (autoExcluded 해제 + 새 add 대신 기존 submission 복원)
6. **이전 핸드오프 4-2 이월** — `isSystemOperator()` 함수 본체 제거 / 자동 승인 후처리 UI 3종 / CycleNew preview row D-3.I

### 4-4. 멀리 (디자이너 결정 후 phase 분리)

7. **raw `text-gray-080/070/060` 일괄 매핑** — 새 토큰 (`fg-strong` / `fg-medium`) 정의 또는 시각 회귀 검토 후 일괄. 분포는 `handoff-2026-05-06.md` § 2-2 표 참조. 본 세션 라운드 1 에서 본문 영역만 핀포인트 처리
8. **B 그룹 audit 잔여** (B2 / B7 / B8 / B11 / B12) — `docs/stability-audit-B.md` 167줄. P2 부채

---

## 5. 함정 / 주의

1. **라운드 4 의 commit body 가 명시한 "Apps Script doPost 분기 추가" 는 무효** (§ 2-5 사후 검증). 신규 분기 불필요 — 기존 `upsertCycle` / `upsertSubmission` / `appendAudit` 가 자동 처리. 다음 에이전트가 commit body 만 보고 .gs 파일을 건드리지 않도록 주의
2. **9 commit 미푸시 상태** — `git push` 전이라 Vercel 자동 배포 안 됨. 로컬에서만 동작. push 후 사용자 첫 앱 사용 시 syncQueue 자동 flush → Sheets 반영
3. **참가자 제외는 삭제가 아닌 마크** — 모든 표시·집계 경로에서 `!s.autoExcluded` 필터 필수. 새 표/리스트 추가 시 누락 주의. KPI 는 아직 보정 안 됨 (§ 4-3 후속)
4. **`resolveTargetMembers` 를 게이트로 사용 금지** — mode 'org'/'manager' 시 발행 후 인원 변동으로 오판. submission 기반 게이트만 사용
5. **autoExcluded 인 user 를 다시 추가하면 신규 submission 한 벌 더 생성됨** (중복 가능). 재활성화 (autoExcluded 해제 복원) 는 별도 phase. 현재는 단순화
6. **Goals 라우트·메뉴 부활 금지** — 사용자 결정 (`e13eea4`). `src/pages/Goals.tsx` 파일은 보존되어 있으나 진입점 부여하지 말 것. 통합은 TemplateBuilder/CycleNew 안에서
7. **'이전 답변 채우기' 매칭 매핑은 보수적** — text 일치 1순위, 위치 fallback. 템플릿 구조 (section order) 가 바뀌면 매칭 안 됨. 사용자가 빈 답변일 때만 활성 — 덮어쓰기 위험 차단
8. **OAuth Client ID 노출 상태 — 외부 시연 금지** (이월)
9. **Apps Script 위치 = `~/Desktop/apps-script/` (git 비추적)** — main repo 안에 `apps-script/` 폴더 만들지 말 것. canonical 은 Apps Script 편집기 (Google 서버)
10. **세션 시작 시 메모리 4종 모두 읽기** — `project_review_mvp.md` / `feedback_collaboration_style.md` / `reference_apps_script_location.md` / `feedback_figma_to_code.md`

---

## 6. 검증 명령어

```bash
# 빌드 + 타입 체크
npm run build

# 단위 + 통합 테스트
npm test

# 타입체크만 (빠름)
npx tsc --noEmit

# 변경 파일 lint
npx eslint src/path/to/changed.tsx

# Dev 서버 (5174)
npm run dev -- --port 5174

# pre-commit 시크릿 스캐너 (husky 자동 실행)
bash scripts/check-secrets.sh
```

---

## 7. 체크리스트 — 새 에이전트 첫 행동

- [ ] `git log --oneline origin/main..HEAD` 로 미푸시 8건 확인
- [ ] `git log --oneline -20` 으로 직전 흐름 (2026-05-06 push 까지) 파악
- [ ] **직전 핸드오프 `handoff-2026-05-06.md` + 본 문서** 읽기
- [ ] `docs/runbooks/script-properties.md` 정독 — 시크릿 운영 규칙
- [ ] `docs/ui-tokens.md` 정독 — 디자인 시스템
- [ ] `docs/stability-audit-B.md` — B 그룹 audit 잔여 5건
- [ ] `npm run build && npm test` baseline 그린 확인
- [ ] dev 서버 5174 띄우기 (배경)
- [ ] § 4-1 의 push + Apps Script 배포 사용자와 합의 후 진행
- [ ] § 4-3 의 우선순위 항목 사용자와 합의 후 진행

---

## 8. 변경 파일 (본 세션 누적, 8 commit 합산)

```
src/App.tsx                                                       (Goals 라우트 add → revert)
src/components/layout/Sidebar.tsx                                 (목표 관리 메뉴 add → revert)
src/components/review/CycleParticipantsPanel.tsx                  (신설 → 삭제)
src/components/review/OpsCenter.tsx                               (참가자 관리 통합)
src/components/review/OpsTable.tsx                                (onRowRemove prop + 인라인 액션 셀)
src/components/review/QuestionCard.tsx                            (이전 답변 채우기 버튼 + readonly <p>)
src/components/review/AuditLogDrawer.tsx                          (참가자 action 라벨/tone)
src/components/review/modals/AddParticipantModal.tsx              (신설)
src/components/review/modals/RemoveParticipantModal.tsx           (신설)
src/pages/AuditLog.tsx                                            (참가자 action 라벨)
src/pages/MyReviewWrite.tsx                                       (readonly <p> + 이전 회차 매핑 + 본문 색)
src/pages/TeamReviewWrite.tsx                                     (readonly 본문 색)
src/pages/reviews/CycleDetail.tsx                                 (Panel 제거)
src/stores/reviewStore.ts                                         (add/removeCycleParticipant action)
src/types/index.ts                                                (AuditAction 추가)
src/utils/opsCenter.ts                                            (autoExcluded 필터)
src/utils/previousAnswers.ts                                      (신설)
docs/handoff-2026-05-18.md                                        (이 문서)
```

---

## 9. 다음 에이전트가 알아야 할 핵심 결정 (Quick reference)

- **9 commit 미푸시** (8 본문 + 본 핸드오프 + 정정 commit) — § 4-1 사용자 합의 후 push
- **Apps Script 신규 작업 불필요** (§ 2-5 사후 검증) — 라운드 4 의 commit body 명시는 무효. 기존 `upsertCycle`/`upsertSubmission`/`appendAudit` 가 자동 처리
- **Goals 는 별도 메뉴 X, 리뷰 운영 안에서 1회성 통합** — `src/pages/Goals.tsx` 보존되어 있으나 진입점 부여 금지
- **사이클 참가자 관리 = OpsCenter 통합** (헤더 추가 + 행 인라인 제외). 별도 패널 신설 금지
- **참가자 게이트 진실의 원천 = submission** (`!autoExcluded` 필터). `resolveTargetMembers` 는 발행 시점 의도 보존용으로만
- **제외 = autoExcluded 마크, 삭제 아님**. 모든 표시·집계 경로에서 필터 필수. KPI 분모 보정은 별도 phase
- **'이전 답변 채우기' = self 한정, MyReviewWrite 만**. 매칭은 text 정확일치 1순위 / (section order, q.order) fallback. 빈 답변일 때만 활성
- **readonly 답변 본문 색 = `text-fg-default`**. raw text-gray-070 / disabled text-fg-subtlest 배제
- **OAuth Client ID rotate 보류** (외부 시연 시작 전 처리)
- **현재 모드**: 마이너 UI 디테일 case-by-case fix 라운드 지속

---

작성: 2026-05-18 · 12일치 누적 4라운드 wrap-up + push/배포 보류 정리. 새 에이전트는 § 7 체크리스트로 시작 권장.
