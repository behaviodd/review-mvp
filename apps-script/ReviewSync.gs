/**
 * [DEPRECATED] ReviewSync.gs — 이 파일은 사용하지 않습니다.
 *
 * 3.3b-1 기준으로 모든 action은 ReviewFlow.gs 에 통합되었습니다.
 * Apps Script 프로젝트에 이 파일을 추가하면 ReviewFlow.gs 의
 * doPost/doGet 과 충돌하여 인증(verifyLogin)·조직(getOrg) 등 일부
 * action 이 사라지는 문제가 발생합니다.
 *
 * 조치:
 *   1) Apps Script 편집기에서 이 파일(ReviewSync.gs)을 삭제.
 *   2) ReviewFlow.gs 한 파일만 남기고 재배포.
 *   3) 한번 migrate_addMissingColumns() 실행.
 */
